import type { ReactNode } from "react";

/** Municipal module layout — per-service ModuleGate lives under [serviceKey]. */
export default function MunicipalLayout({ children }: { children: ReactNode }) {
  return children;
}
