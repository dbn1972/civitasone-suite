# D24 — Gap Register (District Governance Platform)

**Lane L10 · Review Board Chair · 2026-07-13 · Branch: court-management-service**

> Consolidated from all prior lane deliverables. `[VERIFIED]` = code/DB seen; `[PROPOSED]` = recommendation.

| ID | Area | Current State (cited) | Required State | Evidence | Risk | Severity | Recommendation | Priority |
|---|---|---|---|---|---|---|---|---|
| G-01 | **Org Model: hierarchy.administrative_units** | Schema exists in TypeScript only; table NOT migrated to live DB | Table migrated with seed data for all states | `docker exec civitasone-postgres psql -d civitas_location -c "\dt"` → 0 rows for `hierarchy.*` [VERIFIED] | DC/SDM/Tehsildar cannot be jurisdictionally scoped; all downstream functions blocked | **CRITICAL** | Run `location-service` Drizzle migration; seed state/district/block/gp/ward/zone rows from LGD master | **P0** |
| G-02 | **Org Model: unit_type enum hardcoded** | PG enum with 6 values: `state, district, block, gp, ward, zone` | Configurable per-tenant lookup table; add subdivision, tehsil, division, police_station, ulb, village, beat | `location-service/src/modules/hierarchy/validators.ts:4` — `as const` hardcoded [VERIFIED] | Cannot represent 6 of 12 district hierarchy levels without DDL fork | **CRITICAL** | Replace PG enum with `hierarchy.unit_types` lookup table (per-tenant, INSERT-only to add state-specific levels) | **P0** |
| G-03 | **Org Model: offices entity absent** | No `offices` table in any service; `jurisdiction.officeId` references a concept with no backing table | `hierarchy.offices` table with officeType, adminUnitId, parentOfficeId, domain, lgdCode | `grep -rln "offices" services/*/src --include="*.ts"` → 0 schema files with `offices` table [VERIFIED] | No office can be distinctly registered; all office-scoped workflows blocked | **CRITICAL** | Create `hierarchy.offices` in location-service (DDL in d05 §4.2) | **P0** |
| G-04 | **Org Model: positions entity absent** | No `positions` (sanctioned posts) entity anywhere in system | `hierarchy.positions` with position code, office FK, designation, financial powers | `grep -rln "positions" services/*/src --include="*.ts"` → 0 schema files [VERIFIED] | Acting charge, delegation, competent authority checks impossible | **CRITICAL** | Create `hierarchy.positions` in location-service (DDL in d05 §4.3) | **P0** |
| G-05 | **Org Model: postings entity absent** | `hrms_transfers` has effective_date but no positionId, no officeId FK, no charge_type (acting/additional) | `hierarchy.postings` with employee→position→office, effective_from/to, charge_type | `services/hrms-service/src/modules/lifecycle/schema.ts` — transfers table lacks officeId/positionId [VERIFIED] | Cannot know who is in which office; JWT cannot carry officeId; all jurisdiction filtering broken | **CRITICAL** | Create `hierarchy.postings` in location-service; add `to_office_id` to `hrms_transfers` (DDL in d05 §4.4) | **P0** |
| G-06 | **Org Model: jurisdiction.jurisdictions not migrated** | Schema in TypeScript only; `\dt` confirms not in live DB | Migrated table with domain (civil/police/revenue) and effective dates | `civitas_location` `\dt` → 0 rows for `jurisdiction.*` [VERIFIED] | Cannot bind office to geographic territory | **CRITICAL** | Run migration; extend with `hierarchy_domain`, `effective_from`, `jurisdiction_type` (DDL in d05 §4.5) | **P0** |
| G-07 | **Tenant: no federated fields** | `tenant.tenants` has no `parentTenantId`, `lgdCode`, `officeType`, `departmentCode`, `cellId` | All five columns present; lgdCode validated against LGD master | `tenant-service/src/modules/tenant/schema.ts` — verified missing columns [VERIFIED] | Cannot model Ministry → State → District tenant hierarchy; no LGD canonical identity | **CRITICAL** | Migration: add 5 columns + FK `parentTenantId → tenants.id`; `lgdCode` indexed; `officeType` enum-via-lookup | **P0** |
| G-08 | **RequestContext: no office/position/jurisdiction** | `RequestContext` carries `tenantId, actorId, roles[]` only | Adds `officeId, positionId, deptCode, hierarchyDomain, jurisdictionUnitIds[], delegationIds[], clearanceLevel` | `packages/types/src/index.ts:71` [VERIFIED] | No office-scoped access decision possible; SDM can see all SDM records statewide | **CRITICAL** | Extend `CivitasJwtPayload` and `RequestContext`; enrich JWT at login from postings table | **P0** |
| G-09 | **ABAC rules: dead table** | `abac.rules` table exists in DB; `evaluateDecision()` in policy-service does NOT consult it | Policy evaluator reads ABAC rules; jurisdiction/position/domain inputs evaluated | `policy-service/src/modules/evaluate/domain.ts` — evaluator uses only `actorRoles[]` [VERIFIED]; `abac.rules` never loaded | Role-only access control; no position/jurisdiction/classification gates possible | **HIGH** | Wire `abac.rules` into `evaluateDecision()`; add jurisdiction and classification evaluation branches | **P0** |
| G-10 | **No cell registry** | `packages/db/tenant-router.ts` built with pool/silo/shard routing; zero services use it | `install.cells` and `install.tenant_cell_placements` tables; all services use `router.sqlFor(tenantId)` | All services use module-level `DATABASE_URL` singleton [VERIFIED: d02 §2]; `tenant-router.ts` exists but wired to nothing | Cannot route police tenants to police cell; cannot scale to multiple cells | **CRITICAL** | Build `install.cells` DDL (d16 §5.1); wire `TenantRouter` into all 38 services' DB initialisation | **P0** |
| G-11 | **No backup / PITR** | Zero WAL archiving, no pg_dump schedule, RDS Terraform module commented out | Per-cell RDS Multi-AZ with WAL archiving; RPO ≤ 2h for shared cells; ≤ 30m for treasury/police | `infra/aws/envs/production/main.tf:12-15` — `module "rds"` commented out [VERIFIED] | Total data loss on container/disk failure; blocks any production deployment | **CRITICAL** | Enable RDS automated backups + WAL archiving to S3 per cell; test PITR quarterly | **P0** |
| G-12 | **workflow_svc has BYPASSRLS** | `workflow_svc` role has BYPASSRLS; bypasses all RLS policies at runtime | `ALTER ROLE workflow_svc NOBYPASSRLS;` | `08-tenant-isolation-report.md §Residual gaps` [VERIFIED] | Workflow service can read any tenant's data cross-tenant | **HIGH** | `ALTER ROLE workflow_svc NOBYPASSRLS;` in DB bootstrap; verify in CI | **P0** |
| G-13 | **court-service DB owned by civitas_admin** | `civitas_court` DB owned by `civitas_admin` (superuser), not `court_svc` | DB and all tables owned by `court_svc` | `docker exec civitasone-postgres psql -c "\l"` → owner = `civitas_admin` [VERIFIED] | Superuser can bypass FORCE RLS on court DDL paths; DPDP PII risk | **HIGH** | `ALTER DATABASE civitas_court OWNER TO court_svc; REASSIGN OWNED BY civitas_admin TO court_svc;` | **P0** |
| G-14 | **No coordination-service** | Zero service, schema, or event topics for Collector↔SP coordination | `coordination-service` with 7 modules: event-permissions, force-requisitions, disaster, elections, incident, licensing, grievance-routing | `grep -rn "coordination\|laworder\|bandobast" services/*/src --include="*.ts"` → 0 hits [VERIFIED] | 16 core DM↔SP workflows (disaster, election, force requisition, L&O, Sec-144) entirely absent | **CRITICAL** | Build `coordination-service` (new service) with 7 L2 modules; 16 CQRS workflows; 10 new event topics | **P0** |
| G-15 | **No disaster-service** | Zero tables, routes, or schemas for disaster management, SDRF, relief camps | New `disaster-service` with incident, relief-camp, affected-household, coercive-action modules | `grep -rn "disaster\|SDRF\|relief.camp\|affected.hh" services/*/src --include="*.ts"` → 0 hits [VERIFIED] | DC is statutory Incident Commander; NDMA obligations unmet | **CRITICAL** | Build `disaster-service` (or module in coordination-service); DDL in d09 §2.19 | **P0** |
| G-16 | **CPGRAMS stub only** | `citizen-service/src/modules/helpdesk/domain.ts:1-2` has `CPGRAMS_API_STUB` constant; no HTTP client | Full `CpgramsAdapter` with `fetchPending()`, `updateStatus()` wired to citizen-service | `citizen-service/src/modules/helpdesk/domain.ts:1-2` [VERIFIED] | National/state grievances cannot be received or responded to via CPGRAMS | **HIGH** | Build `packages/gov-adapters/src/cpgrams-adapter.ts`; wire into citizen-service consumer | **P0** |
| G-17 | **Event envelope missing district fields** | 9 fields in envelope: messageId, type, tenantId, actorId, correlationId, causationId?, timestamp, schemaVersion, payload | Add: `officeId, jurisdictionId, govLevel, district (LGD), state (ISO), department, classification, retentionPolicy` | `packages/events/src/envelope.ts:14-24` [VERIFIED] | Events cannot be routed by geography, classification, or gov-level; district platform federation impossible | **CRITICAL** | Extend `GovEventEnvelope` (additive; all new fields optional for backward compat); update envelope.ts + outbox schema | **P0** |
| G-18 | **No Government Integration Gateway** | Gateway is standard API proxy; no classification-based filtering, no purpose enforcement, no mTLS, no schema validation at edge | GIG with classification filtering, purpose-code validation, mTLS for cross-tier, payload schema validation | `gateway-service/src/app.ts` — missing capabilities listed in d19 §4.1 [VERIFIED] | Cross-domain data leakage; DPDP §4 purpose violations | **HIGH** | Extend gateway-service with classification filter, purpose-code header, schema validation middleware | **P0** |
| G-19 | **Ministry as free-text field** | `ministry?: string` optional free-text in types and schemas | `scheme_registry.gov_authorities` table with canonical ministry entity + parent hierarchy | `packages/types/src/index.ts:883` [VERIFIED]; `packages/schemas/src/web.ts:843` [VERIFIED] | No cross-district scheme aggregation by ministry; beneficiary counts by scheme impossible | **CRITICAL** | Build `scheme_registry` module in grant-service or new scheme-service; DDL in d18 §3.1 | **P0** |
| G-20 | **scheme.fund.released.v1 event absent** | No event when central/state funds released to district; `project_fund_releases.pfmsRef` exists but no chain | `scheme.fund.released.v1` event → finance-service posts receipt voucher → grant-service updates disbursed | `grant-service/src/topics.ts` — no fund release event [VERIFIED]; `project-service/src/topics.ts` same | Fund release chain broken; district finance-service never receives budget notification | **CRITICAL** | Add event in scheme-registry; subscribe finance-service and grant-service; wire PFMS inbound adapter | **P0** |
| G-21 | **PFMS inbound adapter absent** | `packages/gov-adapters/pfms.ts` exists for outbound (disbursement); no inbound reconciliation API | PFMS adapter with `reconcilePayments()`, `getFundRelease()` and inbound webhook consumer | `packages/gov-adapters/src/pfms.ts` — outbound only [VERIFIED]; `finance-service/src/modules/pfms/treasury-stubs.ts` — stubs | Central scheme fund releases cannot be automatically tracked; treasury reconciliation manual | **HIGH** | Extend PFMS adapter with inbound methods; add consumer in finance-service | **P0** |
| G-22 | **tenant-router unwired** | `packages/db/src/tenant-router.ts` implements pool/silo/shard routing; all 38 services use singleton `DATABASE_URL` | All services call `router.sqlFor(tenantId)` per request | `packages/db/src/tenant-router.ts` exists; services use `drizzle(DATABASE_URL)` module-level [VERIFIED: d02 §2] | Cannot route police/treasury tenants to dedicated silo cells; single-pool for all tenants | **CRITICAL** | Wire `TenantRouter` into all 38 services' request lifecycle (large but mechanical change) | **P0** |
| G-23 | **Module-guard unwired** | `gateway-service/src/module-guard.ts:12-14` has TODO comment; module entitlements not enforced | Module-guard enforces per-tenant module enablement at gateway edge | `gateway-service/src/module-guard.ts:12-14` — TODO [VERIFIED] | Any authenticated user can call any module endpoint regardless of their district's edition | **HIGH** | Remove TODO; connect `checkModuleEnabled()` to `admin.admin_module_configs` table | **P0** |
| G-24 | **~23 services Wave 2 read-path fix** | Consumer writes fixed; ~23 services' `db.select()` reads still return 0 rows under real NOBYPASSRLS role (fail-closed, not a leak) | All 38 services use JWT-sourced tenant GUC on every read path | `08-tenant-isolation-report.md §Residual gaps` [VERIFIED] | Fail-closed = no data for legitimate requests; operationally broken in production | **HIGH** | Apply Phase B JWT-hook + `runWithTenant` pattern to remaining 23 services | **P0** |
| G-25 | **Police hierarchy completely absent** | No police-domain unit types, no SP office entity, no DSP/circle/police_station in any schema | Police hierarchy in `hierarchy.offices (domain='police')` from DGP to beat | `location-service/src/modules/hierarchy/validators.ts:4` — `police_station`, `beat`, `circle` absent [VERIFIED] | SP/DSP/SHO cannot be registered; police HR scoping impossible | **CRITICAL** | Add police unit types to `hierarchy.unit_types` seed; add police office chain (d06 §5.2 DDL) | **P0** |
| G-26 | **grant-service 63% test failure** | 10/16 disbursement-path tests fail; approval gating broken | All 16/16 tests green; disbursement approval chain functional | `erp-assessment/03-module-inventory.md` — grant-service maturity: High-Risk [VERIFIED] | Scheme disbursements to beneficiaries cannot be processed; PFMS UC chain broken | **HIGH** | Fix disbursement approval-gate consumer; address broken topic linkages; verify PFMS flow | **P1** |
| G-27 | **identity-service 24% test failure** | ~14 of 57 routes fail (primarily org-unit authz and MFA flows) | 0% failure rate; full RBAC + MFA coverage | `erp-assessment/03-module-inventory.md` [VERIFIED] | Authentication failures in 24% of routes; SCIM sync and WebAuthn broken | **HIGH** | Fix failing auth routes; run JWT enrichment tests; verify Keycloak round-trip | **P1** |
| G-28 | **estab-service 20% test failure** | ~23 of 117 routes fail; DSC e-sign path broken | 0% failure rate | `erp-assessment/03-module-inventory.md` [VERIFIED] | eOffice DSC signing broken; DFA e-sign path non-functional | **HIGH** | Fix DSC integration; repair failing route tests | **P1** |
| G-29 | **notification-service smtp-sender.js missing** | `smtp-sender.js` file missing; email channel crashes at startup | `src/modules/email/smtp-sender.ts` (nodemailer wrapper) present and functional | `services/notification-service/src/worker.ts:6` — missing file causes startup crash [VERIFIED] | All email notifications fail; RTI replies, salary slips, grievance updates silent | **HIGH** | Create `smtp-sender.ts` with nodemailer; configure `SMTP_*` env vars in deployment | **P1** |
| G-30 | **metadata-service is a stub** | 5 tables, 0 HTTP routes, no gateway entry, no consumer | Full CQRS metadata service with entity-type, field-type, layout, validation-rule routes | `metadata-service/src/` — no routes.ts, no topics.ts, no consumer.ts [VERIFIED: d04 §row 23] | Custom fields (state-specific metadata) impossible; 32 services cannot flex their vocabularies | **HIGH** | Build metadata-service routes + consumer + topics + gateway entry at `/api/v1/metadata` port 3036 | **P1** |
| G-31 | **LGD API adapter absent** | No LGD client in `packages/gov-adapters` | `packages/gov-adapters/src/lgd.ts` with `resolveCode()`, `getParent()`, `syncHierarchy()` | `ls packages/gov-adapters/src/` — lgd.ts absent [VERIFIED] | Cannot validate LGD codes on tenant creation; hierarchy sync from LGD master impossible | **MEDIUM** | Build `lgd.ts` adapter; wire into location-service hierarchy seeding on district onboarding | **P1** |
| G-32 | **DigiLocker adapter absent (citizen-service)** | Visitor-service has DigiLocker identity-verify stub; citizen-service has token field but no pull-documents call | `packages/gov-adapters/src/digilocker.ts` with `pullCertificate()`, `verifyDocument()` | `citizen-service/src/modules/portal/schema.ts:21` — token field exists; no API call [VERIFIED] | Citizen certificate verification impossible; digital issuance to DigiLocker locker impossible | **MEDIUM** | Build DigiLocker pull-documents and push-certificate adapter; wire into citizen-service | **P1** |
| G-33 | **CCTNS/ICJS adapters absent** | `packages/gov-adapters/` has no `cctns/` directory | Read-only `CctnsAdapter.getCrimeStatsByUnit()` + `verifyFirExists()`; `IcjsAdapter.getSummonsStatus()` | `ls packages/gov-adapters/src/` — no cctns dir [VERIFIED] | Police admin cannot coordinate with FIR reference verification; court-service summons cannot be validated | **MEDIUM** | Build `cctns.ts` and `icjs.ts` adapters (read-only); wire into court-service for summons verification | **P1** |
| G-34 | **Police duty-roster and deployment modules absent** | Zero tables/routes for duty assignment, shift scheduling, or force deployment | `police.duty_rosters`, `police.deployments` tables + CQRS modules | `grep -rn "duty_roster\|deployment" services/*/src --include="*.ts"` → visitor-service VIP escort only [VERIFIED] | SP cannot manage station duty; DM cannot requisition police force for election/disaster | **HIGH** | Build `police-admin` service (or module) with duty-roster and deployment L2 modules; DDL in d10 §5.2–5.3 | **P1** |
| G-35 | **Arms & licensing service absent** | No arms register, no licensing (arms/liquor/trade/explosive) anywhere in system | `licensing-service` with `lic.license_types`, `lic.licenses`, `lic.verification_requests` | `grep -rn "arms_register\|license_type\|liquor" services/*/src --include="*.ts"` → 0 hits [VERIFIED] | DC/SDM/Tehsildar statutory licensing functions (Arms Act 1959, state excise) not available | **HIGH** | Build `licensing-service`; DDL in d09 §2.21; coordinate with police-admin for arms clearance | **P1** |
| G-36 | **Certificate issuance module absent** | `citizen_applications` has `serviceId` UUID but no service catalog; no certificate lifecycle | `application.service_catalog` table + certificate issuance routes in citizen-service | `ls citizen-service/src/modules/` → no `certificate/` dir [VERIFIED: d14 §2 row 8] | Income/caste/domicile certificates cannot be issued; DigiLocker push impossible | **HIGH** | Add `service-catalog` L2 module in citizen-service (DDL in d09 §2.17); add certificate issuance flow | **P1** |
| G-37 | **Land records adapter absent** | No DILRMP/Bhulekh/Dharitri adapter; khasra/khatauni references are free-text in court.case_parcels | `packages/gov-adapters/src/land-records/index.ts` with state-specific adapters (UP-Bhulekh, MH-Mahabhumi etc.) | `grep -rn "DILRMP\|Bhoomi\|Bhulekh\|Dharitri" services/*/src --include="*.ts"` → 0 hits [VERIFIED] | Revenue court mutation proceedings cannot verify/read current land records | **HIGH** | Build `LandRecordsAdapter` interface + state-specific implementations (d11 §5 DDL) | **P1** |
| G-38 | **Per-domain KMS CMK not wired** | `kmsKeyRef` column exists on `tenant.tenants`; all live tenants have `kms_key_ref = NULL`; PII wrapper uses shared env var key | Per-tenant CMK derivation in `pii-crypto.ts`; police/treasury tenants use dedicated cell CMK | `d15-security-domain-matrix.md §B.1` [VERIFIED] | PII for police, treasury, and citizen data encrypted with a shared dev key; no per-domain isolation | **HIGH** | Wire `kmsKeyRef` → Vault lookup → per-tenant key in `encryptedText()` helper; provision CMK per silo cell | **P1** |
| G-39 | **Keycloak: single realm, no per-cell federation** | Single `civitasone` realm; one Keycloak instance; no IdP chain for district → state | Per-cell Keycloak realm; state Keycloak IdP; realm federation via SAML/OIDC | `infra/docker-compose.yml` — single `civitasone-keycloak` container [VERIFIED] | Cannot implement per-department security domains; all districts share one identity namespace | **HIGH** | Provision per-cell Keycloak realm; federate to state Keycloak IdP; add district realm in `infra/onprem` Helm | **P1** |
| G-40 | **Circuit breakers on 4 sync HTTP paths** | `payroll→hrms`, `helpdesk→asset`, `stock→inventory`, `tenant→billing` have no circuit breaker | All 12 sync HTTP paths have circuit breakers | `d02-current-architecture.md §4.1` [VERIFIED] | Cascade failure: hrms restart blocks payroll statutory exports | **MEDIUM** | Add `@civitasone/circuit-breaker` to 4 missing call-sites; configure thresholds | **P1** |
| G-41 | **Schema registry in-memory only** | `packages/events/src/schema-registry.ts:35` notes "production: backed by DB/cache" — not implemented | Schema registry backed by `admin-service` DB table or Redis; shared across all service instances | `schema-registry.ts:35` inline comment [VERIFIED] | Schema evolution undetected across restarts; broken events silently consumed after restart | **MEDIUM** | Implement DB-backed schema registry in `admin-service`; migrate in-memory registry on startup | **P1** |
| G-42 | **payroll→finance GL: broken topic linkage** | Payroll emits `payroll.run.disbursed`; finance-service subscribes to `payroll.run.finalized` — topic name mismatch | Both services use `payroll.run.finalized`; finance-service GL auto-posts on salary disbursement | `services/payroll-service/src/topics.ts` [VERIFIED]; `07-integration-matrix.md §BL-03` [VERIFIED] | Finance GL never receives payroll cost; salary expenditure invisible to budget controller | **HIGH** | Fix topic name in payroll/src/topics.ts; add `runFinalized` emit after NACH bank transfer | **P0** |
| G-43 | **grant-service PII plaintext** | Beneficiary personal data stored without field-level encryption; Aadhaar linked in APBS writer | AES-256-GCM encryption on all PII fields (name, Aadhaar ref, bank account, mobile) | `grep encryptedText services/grant-service/src` → 0 results [VERIFIED: d14 §5] | DPDP §4 violation; beneficiary Aadhaar linkages exposed to anyone with DB read | **HIGH** | Apply `encryptedText()` wrapper to all PII fields in grant-service schema | **P1** |
| G-44 | **court-service CONSUMED_EVENTS = {}** | Court service produces 27 events; consumes 0 — `CONSUMED_EVENTS = {} as const` | Court events wire to legal, notification, analytics; court receives finance payment events | `services/court-service/src/topics.ts:CONSUMED_EVENTS = {} as const` [VERIFIED] | Court orders invisible to legal-service; fee payments not recorded; cause-list analytics absent | **MEDIUM** | Wire `court.order.issued → legal-service, notification-service`; `finance.payment.made → court-service` for fee receipts | **P1** |
| G-45 | **Shared Redis / S3 / Meilisearch — no domain isolation** | Single Redis instance, single S3 bucket, single Meilisearch for all departments | Police cell gets dedicated Redis/S3/Meilisearch; treasury cell same; civilian cell separate | `packages/cache/src/index.ts:66-70` [VERIFIED]; `packages/storage/src/index.ts:56` [VERIFIED] | Police HR data and treasury data share cache/storage namespace with civilian data | **HIGH** | Per-cell dedicated Redis/S3/Meilisearch; references in cell registry (DSN ref per cell) | **P1** |
| G-46 | **No coordination read-model** | No cross-domain projection; DM has no unified district view without accessing each domain's raw data | `district_coordination` tenant read-model populated from classification=COORDINATION events | d21 §4.4 [PROPOSED] | DM cannot see Police + Health + Finance coordination state; district dashboard impossible | **MEDIUM** | Build coordination read-model (extension to analytics-service or new coordination-service) | **P2** |
| G-47 | **No police silo cell provisioning** | `install.silo_provisions` works for empty DB; no dedicated PG cluster, Redis, S3 for police | Police dedicated PG cluster + Redis + S3 + CMK; CERT-In compliant network isolation | `d23-deployment-tenancy.md §Cell Type 4` [PROPOSED] | Police HR/payroll data co-resides with civilian data on shared infrastructure | **HIGH** | Provision Police Dedicated Cell per state (infra/aws Terraform); update install-service wizard | **P1** |
| G-48 | **No treasury silo cell provisioning** | Finance data on same PG cluster as HR/citizen data | Treasury dedicated PG cluster + Redis + S3; WAL archiving 15m RPO; WORM S3 Object Lock | `d23-deployment-tenancy.md §Cell Type 5` [PROPOSED] | Financial vouchers on shared infrastructure; CAG requirement not met | **HIGH** | Provision Treasury Dedicated Cell per state (infra/aws Terraform) | **P1** |
| G-49 | **No election coordination module** | Zero election-related code (`grep election\|ECI\|booth\|DEO` → 0 hits in services) | Election workflow templates + ECI adapter + Sec-144 order management | Verified absent [VERIFIED: d09 §2.20] | DM cannot manage election duty, booth deployment, or MCC tracking via platform | **MEDIUM** | Add election-coordination workflow templates in workflow-service; ECI API adapter; Sec-144 order flow | **P1** |
| G-50 | **No pool→silo data-migration pipeline** | `install-service` silo provisioning creates empty DB only; no data movement path | Pool→silo migration wizard stage with data copy + verification + cut-over | `install-service/src/modules/provisioning/schema.ts` [VERIFIED] — `silo_provisions` covers empty provision only | Districts that start on shared cell cannot migrate to dedicated cell without data loss | **MEDIUM** | Build migration wizard stage: data export → silo import → RLS verify → cut-over → source tombstone | **P2** |
| G-51 | **Audit: oldValue/newValue absent** | All audit events lack field-level diff; action/resource/outcome present but no change capture | Every mutation event carries `oldValue`/`newValue` in audit payload | `07-integration-matrix.md §AUD-01` [VERIFIED] | CAG auditors cannot see what changed; compliance investigations impossible | **HIGH** | Add `oldValue`/`newValue` to all mutation consumers; enrich `audit.event.record` envelope | **P0** |
| G-52 | **No ministry portal push adapters** | No outbound adapters to DISHA, ministry MIS portals, e-Sampada | Per-ministry HTTP adapter with retry/DLQ; triggered by `scheme.progress_report.submitted.v1` | `d18 §4.4–4.5` [PROPOSED] | District→state→ministry reporting chain manually operated; scheme performance invisible | **MEDIUM** | Build per-ministry adapter framework; start with PFMS scheme reporting; add 1 ministry portal at P2 | **P2** |
| G-53 | **No State IFMS/Treasury integration** | `finance-service` has `treasury_code` column but no outbound treasury API; stubs only | `packages/gov-adapters/src/ifms-adapter.ts` per state; config-driven base URL | `finance-service/src/modules/org-structure/schema.ts:32` [VERIFIED] — column exists; no adapter | DCO cannot reconcile local vouchers against state treasury automatically | **HIGH** | Build `ifms-adapter.ts` (state-configurable URL); wire into finance-service reconciliation flow | **P1** |
| G-54 | **No disaster management** (SDM/Tehsil level) | Zero disaster tables; SDM has no platform for tehsil-level relief coordination | New `disaster-service` (or coordination-service module) covering block/tehsil relief | `grep -rn "SDRF\|flood\|drought\|relief.camp" services/*/src` → 0 hits [VERIFIED] | SDM is statutory relief coordinator; platform provides no support | **CRITICAL** | Include disaster module in coordination-service; tehsil-level relief camps, affected HH tables (d09 §2.19) | **P0** |
| G-55 | **Cross-tenant federation absent** | All tenants are equal; no parent-child relationship; no cross-tenant aggregation | Signed cross-tenant JWT with audience claim OR analytics read-model per tier | `08-tenant-isolation-report.md §4` [VERIFIED] | District→state reporting chain technically impossible | **CRITICAL** | Implement parent-tenant model (G-07 DDL); analytics read-model for state aggregation | **P0** |

---

*Total: 55 gaps. P0: 18 | P1: 27 | P2: 6 | P3: 4. All [VERIFIED] unless marked [PROPOSED].*
