# D28 — District Governance Platform Readiness Scorecard

**Lane L10 · Review Board Chair · 2026-07-13 · Branch: court-management-service**

> Scale: 0 = absent/broken, 5 = partial/foundational-only, 10 = production-ready.  
> Lane scores used as inputs where available: L02=2, L05=3, L09=5. Others estimated from deliverable evidence.  
> **Existence ≠ points.** A schema in TypeScript that is not migrated to the DB scores 1–2 at most.

| # | Dimension | Score | Evidence (Strength) | Gap | Risk | Required Change |
|---|---|---|---|---|---|---|
| 1 | **District Org Model** (offices, positions, postings, delegation) | **1/10** | Unit types in TypeScript (`state/district/block/gp/ward/zone`); `parentId` on admin-units [VERIFIED] | `hierarchy.*` NOT MIGRATED to DB; `subdivision/tehsil/village/police_station` absent from enum; NO offices, positions, or postings entities anywhere; `RequestContext` carries no `officeId` [VERIFIED: `packages/types:71`] | CRITICAL — every downstream score is blocked | Run migrations; replace PG enum with lookup table; build offices/positions/postings DDL (d05/d06 §4) |
| 2 | **Administrative Geography** (LGD-anchored, multi-level, configurable) | **2/10** | `hierarchy.administrative_units` schema with `lgdCode` field; 6 unit types in code; LGD concept acknowledged | Tables not in DB; missing 6+ levels; `unit_type` is a hardcoded PG enum — adding `tehsil` requires DDL deployment; LGD API adapter absent | HIGH — cannot register any district office without this | Migrate tables; build `unit_types` lookup; seed from LGD master API (d11 §8 DDL) |
| 3 | **Collectorate Capability** (internal ERP: eOffice, HR, finance, procurement, workflow, meetings) | **4/10** | eOffice complete (CSMOP file system, DFA, noting, DSP numbering); finance HOA/DDO/PAO present; procurement GFR-compliant; workflow BPMN engine; meeting governance complete [VERIFIED: memory + d09] | No district-specific functions: disaster absent, CPGRAMS stub, no service catalog for certificates, no jurisdiction-scoped access, no org model to scope SDM from DC | HIGH — module works as single-office internal ERP; fails as district platform | Org model (G-01–G-06); CPGRAMS adapter; certificate module; disaster module; district dashboard |
| 4 | **SP Office Capability** (police HR, payroll, eOffice, budget, procurement) | **2/10** | Generic HRMS, payroll, procurement, eOffice, finance work for any department; police HOA codes could be seeded | Police hierarchy not in DB; NO `sp_office`, `dsp_office`, `police_station`, `beat` unit types; `hrmsEmployees` lacks `cadre` column; police security domain absent; Police cell not provisioned | CRITICAL — SP cannot be registered; police HR/payroll cannot be scoped | Police hierarchy (d06 §7); police cadre columns; security_domain gate; Police Dedicated Cell |
| 5 | **Police Station Capability** (duty roster, arms register, deployment, inspection) | **1/10** | Generic stores/procurement/eOffice works if office entity existed | `police_station` unit type absent from enum; no duty-roster, no arms-register, no deployment-management, no inspection module — ALL absent [VERIFIED: d10 §2A — 0% coverage on 5 key modules] | CRITICAL — police station cannot be represented or managed | Police-admin service (d10 §5); police hierarchy DDL; Police cell |
| 6 | **SDM / Tehsil Capability** (revenue court, mutation, grievance routing, jurisdiction fence) | **1/10** | Revenue court preset exists in court-service code; tehsildar/SDM court types configured in presets.ts [VERIFIED: `court-service/src/modules/config-registry/presets.ts`] | `subdivision` and `tehsil` absent from unit_type enum; `hierarchy.administrative_units` not migrated; SDM office entity absent; jurisdiction fence impossible without officeId in JWT | CRITICAL — SDM cannot be scoped to their sub-division; cannot run revenue court without org model | Org model migrations; revenue court preset activation; land-records adapter; CPGRAMS routing to SDM |
| 7 | **BDO / Panchayat / Rural Development** (scheme monitoring, MNREGS, beneficiary management, geo-tagging) | **2/10** | `block` and `gp` unit types in code (not migrated); project-service has WBS, geo-tagging, scheme components; grant-service has beneficiary, UC, PFMS refs [VERIFIED] | `block`/`gp` not in DB; no BDO office entity; scheme-registry absent; PFMS inbound absent; `ministry` is free-text; project.location free-text (no unit_id FK) | HIGH — scheme monitoring is a core RD function; none of the linkages work | Org model; scheme-registry module; PFMS inbound adapter; project.location FK fix |
| 8 | **Line Department Onboarding** (Health, Education, Agriculture, PWD, etc.) | **3/10** | 15 generic modules (HRMS, payroll, finance, procurement, eOffice, workflow, asset, stock, contract, citizen, legal, analytics, knowledge, meeting, notification) work for any department with configuration [VERIFIED: d13 — 15 modules rated "Fully-available"] | 5 P0 enablers missing: org model (no department isolation), module-guard unwired, metadata-service stub (0 routes), per-department KMS absent, no automated onboarding wizard | HIGH — departments can be manually configured but automation is absent; multi-state rollout requires metadata-service | Org model; module-guard; metadata-service build; automated onboarding workflow; per-dept KMS |
| 9 | **State Hierarchy Integration** (DC→Divisional Commissioner→CS reporting chains) | **1/10** | `tenant.tenants` has `edition`, `orgCategory`, `region` fields; analytics-service has facts consumer | No `parentTenantId`; no state control plane cell; no cross-tenant aggregation; no per-`govLevel` report grouping in report-service; no parent-child tenant relationship anywhere [VERIFIED: d17 §3] | CRITICAL — all upward reporting chains are technically impossible | Parent-tenant model; state control plane cell; cross-cell event router; report-service `govLevel` parameter |
| 10 | **Ministry Integration** (scheme funding, UC chain, progress reporting, audit observations) | **1/10** | `project_fund_releases.pfmsRef` field; `grant_uc_statements` table; PFMS outbound adapter (outbound payment) [VERIFIED] | Ministry is free-text; no `scheme_registry.scheme_masters`; no `gov_authorities` entity; `scheme.fund.released.v1` event absent; PFMS inbound absent; no ministry portal push adapters; no data-sharing agreement governance | CRITICAL — all ministry-to-district fund flows are manual and untracked | scheme-registry module; ministry authority entities; PFMS inbound; `scheme.fund.released.v1` event; ministry push adapters |
| 11 | **Loose Coupling** (event-driven, no cross-service DB, idempotent consumers) | **6/10** | 62 active async event pairs; transactional outbox + relay; `_inbox.processed` markProcessed-first; zero cross-service DSN; 0 cross-schema Drizzle imports; DLQ with CloudWatch alarms [VERIFIED: d02] | 6 broken topic linkages (payroll→finance GL, billing→finance revenue); 27 court events unconsumed; schema registry in-memory only; 4 sync HTTP paths without circuit breaker; bidirectional hrms↔payroll with one direction missing CB | MEDIUM — architecture is sound; gaps are fixable | Fix 6 broken topics; wire CB on 4 paths; persist schema registry; add court event consumers |
| 12 | **Domain Ownership** (each service owns its data; no cross-service SQL) | **7/10** | DB-per-service enforced (35 DBs); zero cross-prefix Drizzle imports found; zero cross-schema JOINs; CI greps enforced [VERIFIED: d02 §1] | No cross-domain data-sharing agreements; no field-level data classification; no purpose-based access (DPDP); grant-service PII plaintext; `abac.rules` dead table | MEDIUM — DB isolation strong; data governance weak | Data-sharing agreement table; purpose-code enforcement; abac.rules evaluator; PII encryption in grant-service |
| 13 | **Security Domain Isolation** (police vs civil vs treasury; no cross-domain data visibility) | **2/10** | FORCE RLS + NOBYPASSRLS per-service roles; fail-closed behaviour [VERIFIED: 08-tenant-isolation-report.md]; `kmsKeyRef` column exists on tenant table | Single shared Redis/S3/Meilisearch; `kmsKeyRef = NULL` for all live tenants; no `security_domain` column on hrms_departments; no domain-level routing in JWT; no Police/Treasury Dedicated Cell provisioned [VERIFIED: d15 §B.1] | CRITICAL — police data shares cache/storage namespace with civil data | Police/Treasury silo cells; `security_domain` RLS gate; per-domain CMK; dedicated Redis/S3 per cell |
| 14 | **Identity Federation** (office-scoped JWT, posting-based auth, per-cell Keycloak) | **3/10** | Keycloak RS256 OIDC working; `tid` claim authoritative (SEC-P0 fix `2ba2911`); SCIM module present; SAML module present [VERIFIED: d23b §1.1] | JWT carries only `tenantId` + `roles[]`; no `officeId`/`positionId`/`jurisdictionId`; evaluateDecision ignores `abac.rules`; single `civitasone` realm (no per-cell federation); ABAC rules dead table [VERIFIED: d23b §1.2–1.3] | HIGH — access decisions cannot be office-scoped or jurisdiction-scoped | Extend JWT with office/posting claims; wire abac.rules into evaluator; per-cell Keycloak realm federation |
| 15 | **Workflow Architecture** (BPMN engine, delegation, SLA, DMN, multi-level approval) | **6/10** | workflow-service: BPMN definitions, node-level SLA, delegation, DMN decision tables, round-robin/least-loaded/hierarchy assignment strategies; 9/9 tests pass [VERIFIED: memory] | No district-specific workflow templates (Sec-144, procession permission, force requisition, disaster declaration); workflow approval chains resolve by user role, not position hierarchy (no office-scoped assignment); `workflow_svc` BYPASSRLS (infra misconfig) | MEDIUM — engine is complete; templates and position-based routing absent | Seed magisterial workflow templates; wire position-hierarchy routing; fix BYPASSRLS |
| 16 | **API Architecture** (REST, schema-first, versioned, authenticated, rate-limited) | **6/10** | 38 services with Fastify REST; gateway JWT edge + rate-limiting + quota + circuit-breakers; 44 routes in gateway registry; zod validation at route boundary [VERIFIED: d02] | No OpenAPI/schema-first generation; payload schema validation NOT wired at gateway; no `officeId`/`jurisdictionId` injection at gateway; no mTLS for cross-tier calls; no purpose-code enforcement; no replay protection | MEDIUM — API is functional; governance layer thin | Wire schema validation; inject office context headers; add mTLS for cross-cell; add purpose-code middleware |
| 17 | **Event Architecture** (outbox, idempotent, classified, geo-tagged, schema-versioned) | **5/10** | Transactional outbox (packages/outbox); idempotent consumers (`_inbox.processed`); DLQ + CloudWatch; schema registry present (in-memory); 62 event pairs [VERIFIED: d02] | Event envelope missing 8 district-platform fields (`officeId`, `jurisdictionId`, `govLevel`, `district`, `state`, `department`, `classification`, `retentionPolicy`); schema registry in-memory (lost on restart); 45 new district event topics needed; 6 broken linkages | HIGH — backbone is sound; classification and geo-routing absent | Extend envelope; persist schema registry; add 45 new topics; fix 6 broken linkages |
| 18 | **Auditability** (immutable audit trail, field-level diff, cross-domain exchange audit, retention policy) | **5/10** | audit-service present with para state-machine, CAG categories, hash chain; all mutations should emit `audit.event.record`; court-service evidence SHA-256 tamper-chain [VERIFIED] | `oldValue`/`newValue` absent from all financial audit events; actor roles never captured in audit payload; plugin executions not audited; no cross-domain exchange audit (`exchangeId`, `legalAuthority`, `fieldsShared` absent); retention policy uniform 180d (insufficient for legal/election records) [VERIFIED: d20 §1 AUD-01–AUD-03] | HIGH — audit exists but is incomplete for CAG/RTI requirements | Add field-level diff to all mutation consumers; add cross-domain exchange audit schema; implement per-event retention policy |
| 19 | **Configurability** (no hardcoded state structures, vocabulary driven by config, edition-based module control) | **3/10** | `theme-service` per-tenant configurable; `admin.feature_flags` table; `tenant.settings JSONB`; court-service config-registry (presets.ts) fully config-driven; `admin.admin_module_configs` for module enable/disable [VERIFIED] | 32/38 services hardcode role names, enum vocabulary, govt-level labels in TypeScript const arrays; metadata-service is a stub (0 routes, 0 gateway entry); module-guard is unwired; no automated terminology configuration per state | HIGH — non-configurable vocab = code fork per state | Build metadata-service; migrate hardcoded vocabs; wire module-guard; add state-specific config wizard |
| 20 | **Scalability** (cell-based, per-tenant routing, queue fairness, bulkhead isolation) | **4/10** | CQRS/outbox pattern supports horizontal scaling; SQS FIFO MessageGroupId=tenantId for queue fairness; `packages/db/tenant-router.ts` with pool/silo/shard routing built [VERIFIED] | Tenant-router unwired (all services use singleton `DATABASE_URL`); no cell registry; no bulkhead per tenant domain; no queue-fairness enforcement across cells; no performance test results published; no per-tenant connection-pool limit | HIGH — architecture designed for scale but wiring absent | Wire tenant-router; build cell registry; add per-tenant connection pool limits; run 1000 TPS load test |
| 21 | **Reliability** (circuit breakers, retry, DLQ, DR, PITR) | **3/10** | `packages/circuit-breaker` built; used on 8/12 sync paths; DLQ per topic with CloudWatch alarms; SQS visibility timeout as retry mechanism [VERIFIED] | Zero backup/PITR (P0); 4 sync paths missing circuit breaker; no explicit retry with exponential backoff+jitter in consumers; no DR playbook tested; no RTO/RPO SLA published; Terraform RDS module commented out | CRITICAL — zero disaster recovery; any disk failure = total data loss | Enable backup/PITR (immediate); add CB to 4 paths; add jitter retry in consumers; write DR playbook; run quarterly PITR drill |
| 22 | **Data Governance** (purpose-based access, data-sharing agreements, PII classification, DPDP compliance) | **3/10** | `pii-crypto.ts` AES-256-GCM pattern used in court-service, visitor-service, payroll-service; DPDP Aadhaar masking in grant-service [VERIFIED] | `kmsKeyRef = NULL` for all tenants (shared dev key); grant-service PII plaintext; no purpose-code enforcement; no data-sharing agreement table; no field-level disclosure filtering; ABAC dead; no `data_classification` on schema columns | HIGH — DPDP §4 compliance requires purpose limitation which is architecturally absent | Per-tenant CMK; PII encryption in grant-service; purpose-code enforcement; data-sharing agreement governance |
| 23 | **Analytics Architecture** (OLAP separation, cross-district aggregation, ministry dashboards) | **5/10** | analytics-service: facts consumer from cross-service events; dashboards module; KPI tables; 11/11 tests pass [VERIFIED: memory]; report-service: scheduled exports, PDF/Excel | No cross-tenant aggregation (flat tenant model); no cross-cell aggregation; no OLAP store (analytics-service is OLTP-adjacent, not a real data warehouse); no district-level KPI preset; analytics-service DB RLS test failure (`unrecognized configuration parameter "app.tenant_id"`) [VERIFIED: d04 §row 2] | MEDIUM — analytics is functional for single tenant; fails for district/state aggregation | Fix RLS in analytics-service; add district-level KPI presets; build cross-tenant read-model for state aggregation; eventual: OLAP projection to BigQuery/Redshift |
| 24 | **Operational Readiness** (monitoring, alerting, runbooks, DR, backup, SLA) | **2/10** | `packages/observability` provides pino structured logging, Prometheus metrics endpoint; CloudWatch DLQ alarms [VERIFIED] | Zero PITR/backup; no DR playbook; no SLA published; no on-call runbook; Terraform RDS module commented out; ALB, ECS, ElastiCache all commented out in production Terraform; no production infra deployed [VERIFIED: `infra/aws/envs/production/main.tf:12-15`] | CRITICAL — system is dev-only; cannot be used with real government data | Enable RDS, ECS, ElastiCache, ALB in Terraform; enable backup/PITR; write runbooks; publish SLA |
| **25** | **OVERALL DISTRICT READINESS** | **3/10** | Strong internal-ERP substrate (finance, payroll, CQRS, RLS, eOffice, court-service) with genuine government domain awareness and sound architecture | Org model absent from DB; no office/position/posting/jurisdiction; no coordination domain; no backup; no district-level configurability; police/treasury domain isolation absent; ministry integration primitive | **The system is not suitable for district governance deployment today** | See Phase-0 roadmap (d26): 40–65 engineer-sprints to reach limited-district-pilot status |

---

## Score Distribution

| Range | Count | Dimensions |
|---|---|---|
| 1–2 (Absent / Critical gap) | 8 | district-org-model, police-station, SDM/Tehsil, state-hierarchy, ministry-integration, security-domain-isolation, operational-readiness, SP-office |
| 3–4 (Partial / Foundational only) | 8 | admin-geography, collectorate, BDO/panchayat, line-dept-onboarding, identity-federation, configurability, reliability, data-governance |
| 5–6 (Functional for limited scope) | 7 | loose-coupling, workflow-arch, API-arch, event-arch, auditability, analytics-arch, scalability |
| 7–9 (Strong / Near-ready) | 1 | domain-ownership |
| 10 (Production-ready) | 0 | — |

**Weighted average (24 scored dimensions): 3.3/10 → rounded to 3/10 overall.**

---

## Lane Input Scores

| Lane | Deliverables | Score | Key Finding |
|---|---|---|---|
| L01 — Current Architecture | d02, d04 | 4/10 | Sound microservices substrate; org model absent |
| L02 — Org / Admin Hierarchy | d05, d06, d23b | **2/10** (explicit) | Hierarchy not migrated; 1/10 for district pilot on current model |
| L03 — Collectorate / SDM / Tehsil | d09, d11 | 3/10 (est.) | Generic ERP works; district-specific functions absent |
| L04 — Police / BDO / Panchayat | d10, d12 | 2/10 (est.) | Police hierarchy absent; generic HR configurable but untested |
| L05 — Line Depts | d13 | **3/10** (explicit) | 15 generic modules functional; 5 enablers missing |
| L06 — Data Ownership / Security | d14, d15 | 3/10 (est.) | DB isolation strong; domain isolation absent |
| L07 — Integration | d17, d18, d19, d20 | 2/10 (est.) | 45 new event topics needed; coordination/PFMS/CCTNS absent |
| L08 — Federation / Deployment | d16, d21, d22, d23 | 3/10 (est.) | Target architecture defined; cell registry and backup absent |
| L09 — Loose Coupling / Workflow / Config | d09b | **5/10** (explicit) | Event backbone sound; configurability and district envelope weak |
| **L10 (this)** | d01, d24–d28, d00 | **3/10** | Overall district readiness |
