# infra/.env.example — copy to infra/.env and set real values; NEVER commit .env
POSTGRES_ADMIN_PASSWORD=<strong-random-password>
KC_DB_PASSWORD=<strong-random-password>
KC_ADMIN_PASSWORD=<strong-random-password>
KEYCLOAK_SVC_CLIENT_SECRET=<strong-random-secret>
KEYCLOAK_ADMIN_CLI_SECRET=<strong-random-secret>
