apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-civitasone-config
  labels:
    {{- include "civitasone.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
